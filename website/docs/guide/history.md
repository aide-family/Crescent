# 会话历史

会话历史保存你和 Agent 的对话。打开一条历史会话会恢复当时的对话内容。

如果那次会话关联了 SSH 连接，Crescent 会重新打开终端，并按**当前保存的连接配置**登录，而不是沿用当时已经过期的密码或临时环境。关联的连接如果已经被删掉，就不能自动重连，需要先在 [SSH 连接](/guide/ssh) 里补回那台主机。

历史列表可以改标题。一次会话里可以有多次 Agent 运行，列表会标出运行次数。
