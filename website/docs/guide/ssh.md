# SSH 连接

Crescent 可以读取 `~/.ssh/config`，也支持自定义 SSH 连接、登录动作、SSH 参数和密码环境变量。

在同一个工作台里切换：

- 本地终端
- 测试环境
- 生产主机
- 其他远程机器

Agent 以当前选中的连接为现场。换一台机器后再提问，后续命令会落在新的会话上，而不是沿用上一个主机的假设。

需要并行查看另一台主机时，使用 [Agent 工具](/guide/tools) 里的 `open_subterminal`，把专用本地或 SSH 终端停靠出来。
