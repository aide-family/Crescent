---
description: Crescent development must follow pi-dev, Electron, and desktop-app-design
alwaysApply: true
---

# Development standards

Follow these skills as the source of truth. Do not invent APIs or desktop patterns from memory.

Project skills (`.cursor/skills/`): `crescent-host`, `crescent-login-intent`, `crescent-delivery-gate`.

## Host (`crescent-host`)

- Crescent owns SSH, MCP, subterminals, command review, and secrets. Pi is the guest runtime.
- Do not enable `pi-mcp-adapter` or `pi-subagents` as the product plane. Do not load project `.pi/extensions`.
- Do not invoke `electron-egg` / `ee-core`.

## Login (`crescent-login-intent`)

- Open SSH only when the user named a connection, explicitly asked to log in, or the session already has that SSH (including same-host drift reconnect).
- Do not guess lastUsed or the only configured host. `打开` is not login intent.

## Delivery (`crescent-delivery-gate`)

- Non-trivial work: survey first, then implement, then `npm run ci`, then an acceptance checklist. Do not commit unless asked.

## Pi (`pi-dev`)

- Read installed `@earendil-works/pi-*` docs/types in `node_modules` before changing agent code. Keep those packages on one version.
- Import pi only through `src/main/agent/pi-sdk.ts` (`import()`). Never static-import pi into CJS main.
- Custom tools must be allowlisted, truncate large output, and use `withFileMutationQueue` for file writes.
- Put pi-facing tests next to the module (`src/main/agent/*.test.ts`).

## Electron (`electron-dev`, `electron`)

- Renderer: no Node, no filesystem. Main owns OS/DB/secrets. Preload is the only bridge.
- Keep `contextIsolation: true`, `nodeIntegration: false`. Whitelist IPC in preload; validate arguments in main.
- Native modules stay outside the bundler (`asarUnpack` / `external`), loaded at runtime.

## Desktop UI (`desktop-app-design`, `docs/UI_DESIGN_SYSTEM.md`)

- Dense, keyboard-first, quiet operations chrome. No marketing layouts or nested cards.
- Accent is `#13c2c2`. Terminal/code/mermaid use shared tokens. Visible focus rings; Escape closes overlays.

## Electron EGG (`electron-egg`)

- Crescent is electron-vite + electron-builder, not `ee-core`. Do not introduce Electron EGG architecture, plugins, or APIs.
